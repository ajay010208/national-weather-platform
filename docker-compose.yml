version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - backend_data:/app/data
    environment:
      - APP_ENV=development
      - DEBUG=true
    restart: unless-stopped

volumes:
  backend_data:
